# Immersive Design Studio

A portfolio website showcasing immersive design experiences including Virtual Reality, Interactive Installations, and Spatial Audio projects.

## 🚀 Features

- Responsive design with Tailwind CSS
- Dark mode support
- Interactive project showcase
- Modern UI with Space Grotesk font
- Optimized for all devices

## 🛠️ Tech Stack

- HTML5
- Tailwind CSS
- Material Symbols Icons
- Express.js (Node.js server)

## 📦 Installation

```bash
# Install dependencies
npm install

# Start the server
npm start
```

The server will start on port 3000 by default (or the PORT environment variable if set).

## 🌐 Deployment

### Railway Deployment

This project is configured for easy deployment on Railway:

1. Connect your GitHub repository to Railway
2. Railway will automatically detect the Node.js project
3. The app will be deployed and accessible via the provided Railway URL

No additional configuration needed - Railway will automatically:
- Install dependencies (`npm install`)
- Start the server (`npm start`)
- Use the PORT environment variable

### Environment Variables

- `PORT`: Server port (automatically set by Railway)

## 📁 Project Structure

```
├── index.html          # Main HTML file
├── screen.png          # Screenshot/preview image
├── server.js           # Express server
├── package.json        # Node.js dependencies
├── .gitignore         # Git ignore rules
└── README.md          # This file
```

## 🎨 Customization

Edit `index.html` to customize:
- Content and text
- Colors (Tailwind configuration)
- Images and project showcases
- Contact information

## 📄 License

ISC
